package aufgabe6;

import java.util.Vector;

public class Spielfeld {
	private Vector<Figur> felder;
	
	public Spielfeld() {
		felder = new Vector<Figur>(40);
	}
	/*
	public boolean istZugGueltig(int startPosition, int endPosition) {
		if (startPosition >= 0 && startPosition < 40 && endPosition >= 0 && endPosition < 40) {
			
		} else
			return false;
	}*/
	
	public void bewegeFigur(int startPosition, int endPosition) {
		
	}
}
